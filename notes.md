Content delivry network (CDN): group of connected servers spread across diff parts of the world that work together to load websites and digital content faster

Server components can access back-end resources directly

gte: greater than equal to, lt: less than

We cannot use 'select' and 'include' at the same level of the same query. 