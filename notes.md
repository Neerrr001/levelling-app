Content delivry network (CDN): group of connected servers spread across diff parts of the world that work together to load websites and digital content faster

Server components can access back-end resources directly

gte: greater than equal to, lt: less than

We cannot use 'select' and 'include' at the same level of the same query. 

bind(): creates a new function with some arguments already filled in. 

Zod: is a typescript first validation library. Using Zod, we can define schemas we can use to validate data, from a simple 'string' to a complex 'nested obj'
Zod's features: zero dependancies, tiny, works in node.js and all modern browsers, built-in json schema conversion 

An MCP server (Model Context Protocol server) is a software program that acts as a universal bridge, allowing AI models and assistants to securely connect to external tools, databases, and filesystems
